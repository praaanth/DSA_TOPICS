/*
Partition array for maximum sum
intution is very much similar to  max subsequence sum problem
dp[i]=maxsum ending at dp[i]
we check with using the current element in all its window pf size k what is maximum sum we can get till ith index \
store it in the dp[i];

*/
#include <bits/stdc++.h>
using namespace std;


refer leetcode for code
